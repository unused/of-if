#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup' if File.exist? './of_if.gemspec'
require 'of_if'

runner = OfIf::Runner.new
runner.target_dir = ARGV unless ARGV.empty?
runner.()
